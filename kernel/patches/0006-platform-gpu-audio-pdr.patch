diff --git a/drivers/firmware/qcom/qcom_scm-smc.c b/drivers/firmware/qcom/qcom_scm-smc.c
index 574930729..dc340a83f 100644
--- a/drivers/firmware/qcom/qcom_scm-smc.c
+++ b/drivers/firmware/qcom/qcom_scm-smc.c
@@ -210,6 +210,7 @@ int __scm_smc_call(struct device *dev, const struct qcom_scm_desc *desc,
 		res->result[2] = smc_res.a3;
 	}
 
+
 	return (long)smc_res.a0 ? qcom_scm_remap_error(smc_res.a0) : 0;
 
 }
diff --git a/drivers/firmware/qcom/qcom_scm.c b/drivers/firmware/qcom/qcom_scm.c
index fc4d67e4c..bfdfea19a 100644
--- a/drivers/firmware/qcom/qcom_scm.c
+++ b/drivers/firmware/qcom/qcom_scm.c
@@ -634,6 +634,7 @@ int qcom_scm_pas_init_image(u32 peripheral, const void *metadata, size_t size,
 		ctx->size = size;
 	}
 
+
 	return ret ? : res.result[0];
 }
 EXPORT_SYMBOL_GPL(qcom_scm_pas_init_image);
diff --git a/drivers/gpu/drm/msm/adreno/a6xx_gmu.c b/drivers/gpu/drm/msm/adreno/a6xx_gmu.c
index c8711938a..8b8c47683 100644
--- a/drivers/gpu/drm/msm/adreno/a6xx_gmu.c
+++ b/drivers/gpu/drm/msm/adreno/a6xx_gmu.c
@@ -18,6 +18,7 @@
 #include "msm_gpu_trace.h"
 #include "msm_mmu.h"
 
+
 static void a6xx_gmu_fault(struct a6xx_gmu *gmu)
 {
 	struct a6xx_gpu *a6xx_gpu = container_of(gmu, struct a6xx_gpu, gmu);
@@ -175,8 +176,9 @@ void a6xx_gmu_set_freq(struct msm_gpu *gpu, struct dev_pm_opp *opp,
 	if (!gmu->legacy) {
 		a6xx_hfi_set_freq(gmu, perf_index, bw_index);
 		/* With Bandwidth voting, we now vote for all resources, so skip OPP set */
-		if (!bw_index)
+		if (!bw_index) {
 			dev_pm_opp_set_opp(&gpu->pdev->dev, opp);
+		}
 		return;
 	}
 
@@ -1191,7 +1193,6 @@ static void a6xx_gmu_shutdown(struct a6xx_gmu *gmu)
 
 	a6xx_bus_clear_pending_transactions(adreno_gpu, a6xx_gpu->hung);
 
-	/* tell the GMU we want to slumber */
 	ret = a6xx_gmu_notify_slumber(gmu);
 	if (ret)
 		goto force_off;
diff --git a/drivers/gpu/drm/msm/adreno/a6xx_gpu.c b/drivers/gpu/drm/msm/adreno/a6xx_gpu.c
index 242d02d48..47f260af5 100644
--- a/drivers/gpu/drm/msm/adreno/a6xx_gpu.c
+++ b/drivers/gpu/drm/msm/adreno/a6xx_gpu.c
@@ -14,6 +14,7 @@
 #include <linux/pm_domain.h>
 #include <linux/soc/qcom/llcc-qcom.h>
 
+
 #define GPU_PAS_ID 13
 
 static inline bool _a6xx_check_idle(struct msm_gpu *gpu)
diff --git a/drivers/gpu/drm/msm/adreno/adreno_gpu.c b/drivers/gpu/drm/msm/adreno/adreno_gpu.c
index 26db1f4b5..0bfad99e1 100644
--- a/drivers/gpu/drm/msm/adreno/adreno_gpu.c
+++ b/drivers/gpu/drm/msm/adreno/adreno_gpu.c
@@ -21,6 +21,7 @@
 #include "msm_gem.h"
 #include "msm_mmu.h"
 
+
 static u64 address_space_size = 0;
 MODULE_PARM_DESC(address_space_size, "Override for size of processes private GPU address space");
 module_param(address_space_size, ullong, 0600);
diff --git a/drivers/soc/qcom/mdt_loader.c b/drivers/soc/qcom/mdt_loader.c
index b2c0fb55d..a3c9c0170 100644
--- a/drivers/soc/qcom/mdt_loader.c
+++ b/drivers/soc/qcom/mdt_loader.c
@@ -18,6 +18,7 @@
 #include <linux/slab.h>
 #include <linux/soc/qcom/mdt_loader.h>
 
+
 static bool mdt_phdr_valid(const struct elf32_phdr *phdr)
 {
 	if (phdr->p_type != PT_LOAD)
@@ -381,8 +382,9 @@ static int __qcom_mdt_load(struct device *dev, const struct firmware *fw,
 				break;
 		}
 
-		if (phdr->p_memsz > phdr->p_filesz)
+		if (phdr->p_memsz > phdr->p_filesz) {
 			memset(ptr + phdr->p_filesz, 0, phdr->p_memsz - phdr->p_filesz);
+		}
 	}
 
 	if (reloc_base)
diff --git a/include/linux/soc/qcom/pdr.h b/include/linux/soc/qcom/pdr.h
index 83a8ea612..12779ba07 100644
--- a/include/linux/soc/qcom/pdr.h
+++ b/include/linux/soc/qcom/pdr.h
@@ -4,7 +4,7 @@
 
 #include <linux/soc/qcom/qmi.h>
 
-#define SERVREG_NAME_LENGTH	64
+#define SERVREG_NAME_LENGTH	128
 
 struct pdr_service;
 struct pdr_handle;
diff --git a/sound/soc/qcom/sm8250.c b/sound/soc/qcom/sm8250.c
index c4446f98f..835979351 100644
--- a/sound/soc/qcom/sm8250.c
+++ b/sound/soc/qcom/sm8250.c
@@ -14,6 +14,7 @@
 #include "qdsp6/q6afe.h"
 #include "common.h"
 #include "sdw.h"
+#include <sound/cs35l41.h>
 
 #define DRIVER_NAME		"sm8250"
 #define MI2S_BCLK_RATE		1536000
@@ -152,12 +153,32 @@ static int sm8250_snd_startup(struct snd_pcm_substream *substream)
 
 		for_each_rtd_codec_dais(rtd, j, codec_dai) {
 			ret = snd_soc_dai_set_fmt(codec_dai, codec_dai_fmt);
-			snd_soc_dai_set_sysclk(codec_dai, 0, TDM_BCLK_RATE, SNDRV_PCM_STREAM_PLAYBACK);
 			if (ret < 0) {
 				dev_err(rtd->dev, "TDM fmt err:%d\n", ret);
 				return ret;
 			}
 
+			ret = snd_soc_dai_set_sysclk(codec_dai, 0, TDM_BCLK_RATE,
+						     SNDRV_PCM_STREAM_PLAYBACK);
+			if (ret < 0) {
+				dev_err(rtd->dev, "TDM codec sysclk err:%d\n", ret);
+				return ret;
+			}
+
+			/*
+			 * Уровень DAI настраивает только монитор частоты; опорную
+			 * частоту PLL программирует обработчик уровня компонента,
+			 * без него усилитель не поднимает питание.
+			 */
+			ret = snd_soc_component_set_sysclk(codec_dai->component,
+							   CS35L41_CLKID_SCLK, 0,
+							   TDM_BCLK_RATE,
+							   SND_SOC_CLOCK_IN);
+			if (ret < 0) {
+				dev_err(rtd->dev, "TDM codec PLL err:%d\n", ret);
+				return ret;
+			}
+
 			ret = snd_soc_dai_set_channel_map(codec_dai, 0, NULL,
 						  ARRAY_SIZE(cs35l41_tdm_channel_map[j].rx),
 						  (unsigned int *)cs35l41_tdm_channel_map[j].rx);
