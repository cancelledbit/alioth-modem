[Unit]
# pd-mapper runs early and finds nothing to serve yet - "no pd maps available" -
# and the default burst limit then gives up on it for the rest of the boot.  Let
# it keep trying instead: once it does start it stays up.
StartLimitIntervalSec=0

[Service]
RestartSec=5
