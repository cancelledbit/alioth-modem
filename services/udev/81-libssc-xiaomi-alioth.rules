# iio-sensor-proxy reaches the sensors through libssc, over the sensor core's
# FastRPC device.  The mount matrix says how the accelerometer sits in this
# phone relative to the screen.
SUBSYSTEM=="misc", KERNEL=="fastrpc-*", ENV{ACCEL_MOUNT_MATRIX}+="1, 0, 0; 0, 1, 0; 0, 0, 1"
